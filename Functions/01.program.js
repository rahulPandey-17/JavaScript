// function

function onePlusAverage(a, b, c) {
    return 1 + ((a + b + c) / 3);
}

// arrow function

const sum = (num1, num2) => {
    return num1 + num2;
}

let a = 10, b = 20, c = 30;
console.log(onePlusAverage(a, b, c));

let num1 = 20, num2 = 30;
console.log(sum(num1, num2));