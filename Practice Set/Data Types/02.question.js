// object

const item = {
    item : "apple",
    price : 200
};

// we cannot assign item to any other thing now
// item = 45; // will throw a error

// but we can add key-value pairs to the object
item["color"] = "red";

// we can change values inside object
item["item"] = "mango";

console.log(item);